services:
  app:
    image: ${IMAGE_NAME}:${IMAGE_TAG}
    container_name: mcm-orbit
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    stop_grace_period: 30s
